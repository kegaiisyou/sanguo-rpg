# 乱世烽火 · 进度 / 设计对照文档

> 本文档跟踪「设计基线 `GAME_DESIGN.md`」与「实际落地代码」的对照关系，用于收尾盘点与后续开发接棒。
> **用户可见版本**：`LF.CONSTANTS.VERSION`（当前 `20260831q`，见 `shared/config/constants.js`），每次迭代/内容改动后 bump，并同步 `index.html` 中对应 `<script src="...?v=...">` 缓存参数。
> **存档 schema 版本**：`shared/index.js` 的 `defaultSave().version`（当前 `0.2.0`），仅用于存档兼容/迁移，与显示版本无关，切勿改动。
> 主端：网页 H5 `index.html`，唯一数据源：`shared/`。

---

## 〇、一句话结论

**游戏框架已搭建完成，且已形成自洽可玩闭环**：研习真招式 → 剿匪/巡山攒声望 → 声望≥20 斩华雄 → 解锁洛阳凯旋；途中三资源（食/水/精力）有硬性限制。剩余项均为「丰富度扩展」，非框架必需。

---

## 一、P 路线图对照表

| 设计项 | 状态 | 实际落地 | 代码落点 |
|--------|------|----------|----------|
| **P0 战斗系统** | ✅ 完成 | 半手动 + 伤害公式（**百分比减伤模型**：`减伤率=min(0.8, 防御战力×1%)`，防御换算×4，抗数值膨胀）+ Buff/Debuff + 华雄 Boss；13 招 + 5 敌 + 4 AI | `shared/combat/engine.js`、`shared/data/martial.js`、`shared/data/enemies.js`、`index.html` 战斗 UI |
| **P1 背包（子集）** | ✅ 完成 | `items` 字段 + 掉落堆叠入库 + 行囊展示（`skillTags` 同屏展示武学） | `shared/index.js` `defaultSave().items`、`index.html` 行囊弹窗 |
| **P2 武学合一（最小版）** | ✅ 完成 | 研习界面接 `learnedMartial`，按 13 艺线分组 + 门槛锁 + 学招涨艺线；行囊/任务显示真招式；发力技巧可装配 | `index.html` `openLearn()`、`skillTags()`、`shared/data/martial.js`（MARTIAL_ARTS） |
| **P3 声望框架** | ✅ 完成 | 8 档称号、状态栏、华雄门控（声望≥20）、任务弹窗展示 | `index.html` `repTitle()`、`renderStatus()`、任务弹窗 |
| **P4 主线门控** | ✅ 完成 | 胜战真实 `addReputation`（山贼+2/流寇+4/盗匪+5/华雄+20）；`state.quest` 计数；华雄→洛阳解锁链（新增洛阳城房间） | `index.html` `endCombat()` 胜利分支、`shared/story/rooms.js`（luoyang 房间） |
| **资源硬性限制** | ✅ 完成 | 饥饿（食/水耗尽）持续扣血；精力耗尽封锁行动；战斗耗精力；状态栏红字警告；三处补「休整」防卡死 | `index.html` `maybeStarve()`、`exert()`、`move()`、`startCombat()`、`renderStatus()` |
| **清理** | ✅ 完成 | 移除木人桩入口；调试台改为 `?dev=1` 才显示；底部 dock「更多」→「设置」按钮 | `shared/story/rooms.js`、`index.html` 设置弹窗 |
| **UI 交互重构** | ✅ 完成 | 场景对象卡片式交互：人物/建筑/家具/出口以卡片呈现，点击展开具体动作（交谈/攻击/休整/移动）；手风琴收起；告别平铺长按钮列表 | `index.html` `ROOM_OBJECTS` 配置 + 重写 `buildActions()` + `.obj-card` CSS |
| **战败封锁** | ✅ 完成 | 战败设 `state.defeated` 封锁一切行动（仅留「休整」）；`endCombat` 清按钮并标记引擎 `result='ended'`，彻底阻断残留 `playTurn`（崩拳等不再可点） | `index.html` `endCombat()`、`buildActions()`、`actRest()` |
| **P2 完整版** | ✅ 完成 | ① 艺线经验由战斗出手累积（`engine.getLineGains`→`endCombat` 胜利结算，`(lv+1)*40` 升级，学招仍 +1）；② 艺线等级→攻/命中/暴击/内力/身法（`effectiveStats` 注入，`atk +0.6/级`、`hitRate 0.92+0.002/级`、`critRate 0.003/级`，内功→内力、轻功→身法）；③ 境界突破效果接入（`engine._buildArt`/`_parseBonus` 解析 `breakthrough` 文本→伤害%/暴击%/破甲/破防/眩晕/连击/必暴击，攻防暴击随境界提升）；④ 命中判定系统（玩家 `hitRate`、敌人 0.9，未命中不造成伤害）；⑤ 研习界面显示每式当前境界 | `shared/combat/engine.js`（`init`+`_buildArt`+`_parseBonus`+`calcDamage`+`_resolveAction`+`getLineGains`）、`index.html`（`effectiveStats`/`startCombat`/`endCombat`/`renderStatus`/研习界面） |
| **P1 装备系统** | ✅ 完成 | 武器/防具/饰品/坐骑四槽 + 5 档品质（凡品→神兵）+ 耐久衰减损毁；战斗掉落生成、行囊装备/卸下、有效属性并入战斗（攻/防/血/内/速） | `shared/data/items.js`、`shared/data/enemies.js`（drop.equip 配置）、`shared/combat/engine.js`（getDrop 生成）、`index.html`（`effectiveStats`+行囊装备面板+耐久衰减） |
| **P3 善恶双轨** | ✅ 完成 | ① `karma` 单值拆为 `chivalry`/`notoriety` 独立双轴（互不抵消，旧档自动迁移）；② 事件抉择按 `choice.moral` 累积侠义/凶名（含「趁火打劫」「强夺」等凶名选项）；③ 阈值解锁（侠义≥30→侠义委托、凶名≥30→影门悬赏、双≥40→枭雄隐藏事件）；④ NPC 态度系统（`npcAttitude` 依 `align` 阵营+双轴出 敬重/友善/平常/戒备/敌视/亲近/看重，敌视拒谈）；⑤ 风评称号 `moralTitle`（清流义士/绿林枭雄/亦正亦邪·枭雄…）；⑥ 状态栏与行囊展示双轴+风评；⑦ 调试台双轴±10/清零 | `shared/index.js`（`defaultSave`）、`shared/story/events.js`、`shared/story/dialogues.js`、`shared/story/rooms.js`、`index.html`（`normalize`/`moralLabel`/`moralTitle`/`addChivalry`/`addNotoriety`/`afterMoral`/`npcAttitude`/`talk`/`runEvent`/`renderStatus`/行囊/调试台） |
| **历法系统** | ✅ 完成 | 农历（月/日/月相阶）+ 公历（年/月/日/星期）双轨；时辰（12 时辰，子时居顶午时居底、卯东酉西）；状态栏常驻古历 `光和元年·腊月十五 + ☀晴`；钟表弹窗附公历·星期对照与「点他处关闭」 | `index.html`（`deriveCalendar`/`syncCalendar`/`renderStatus`/钟表弹窗）、`shared/index.js`（`defaultSave` 历法字段与 `normalize` 回填） |
| **天候系统** | ✅ 完成 | 8 种天候（晴/多云/阴/微雨/大雨/雪/雾/风），跨日随机变化；状态栏图标+名称；钟表弹窗可选查看（已精简：移除意境描述，仅留名称+图标） | `index.html`（`WEATHERS`、状态栏 `st-wx`、`advanceTime` 跨日换天候） |
| **NPC 交互重构（右键式浮层菜单）** | ✅ 完成 | 点击 NPC/物件在**光标处弹出不带描述的紧凑浮层**（交谈/观察/攻击 + 对象自带动作，带淡入缩放动画）；遇屏幕边缘自动翻转避免溢出；点空白 / `Esc` / 滚动场景自动收起 | `index.html`（`openObjMenu`/`positionMenu`/`collapseObjPanel`、`.obj-menu` CSS） |
| **清理·测试实体** | ✅ 完成 | ① 移除主营「操练场·木人桩」（`spar_dummy`→`enemies.dummy` 不可达死代码待清）；② 移除黑山寨聚义厅「【测试】多按钮演示」NPC（5 按钮排版演示）。两者测试使命已完，游戏内不再出现 | `index.html` `ROOM_OBJECTS`（camp / ji_heishan_zhai 房间） |
| **时间锚点订正** | ✅ 完成 | 年号由「中平元年（184）」改为「**光和元年（178）**」，与 `GAME_DESIGN.md` 时间锚点（光和年间·乱象初显之世）一致；序章文案同步 `汉灵帝光和年间`；旧档缺年号补默认 `光和` | `shared/index.js`（eraName/adYear）、`index.html`（历法派生/normalize/状态栏/钟表兜底）、`shared/story/dialogues.js`（序章） |
| **山河志·空间地图** | ✅ 完成 | 「山河」弹窗由竖排列表改为**按出口方位铺展的网格空间地图**：① 节点按 `MAP_COORDS`(col/row) 定位（北在上、东在右），连线表出口通达关系，当前所处红点高亮、通达之路实线加深、悬停显「〔通达〕方向·地名」；② 加**山河底色**（中原/洛阳/幽州区域色块 + 网格纹 + 羊皮纸底）、**距离刻度**（1 格 ≈ 60 里，随缩放实时变化）；③ **默认以当前所处为中心**、支持**任意拖拽平移**、**滚轮 / 双指缩放**（0.5×–3×）；④ 调试台「设置出生点」复用同一空间地图（点节点即设出生点并传送），修复旧列表样式丢失后的拥挤问题 | `index.html`（`MAP_COORDS`/`MAP_CELL`/`buildMapHTML`/`initMap`、`MAP_COORDS` 渲染、`.map-canvas/.map-viewport/.map-region/.map-scale` CSS） |
| **P5 影门** | ⬜ 仅骨架 | 设计文档仅占位，代码未实现 | — |
| **P6 家族兴衰** | ⬜ 仅骨架 | 设计文档仅占位，代码未实现 | — |
| **标题屏 UI** | ✅ 完成 | 长江落日水墨标题屏：四入口（仗剑入世/拾卷续缘/览物志/整肃）；三档存档（lf_save_1~3，旧档 lf_save_v1 自动迁第一档）；死亡弹「殒落」回标题屏（可拾卷续缘或读档续命）；图鉴（武学/门派/贼寇名录）与设置标签页（画面/声音/游戏；标题页同款但无调试台）弹窗 | `index.html`（#title / openModal newgame·load·codex·settings / enterGame·showTitle / saveToSlot·slotMeta）、`shared/index.js` |
| **招牌联动** | ✅ 完成 | 店铺招牌随机生成（如「福兴杂货铺」），掌柜/店主台词自称「福兴号」等（13 处闲谈嵌入，无招牌回退建筑名），增强代入感 | `index.html`（`bldZihao()`）、`shared/data/markets.js`（sign 生成） |
| **店铺营业时间** | ✅ 完成 | 建筑内动作按 <code>when</code> 时辰显隐（day=卯~酉 / night=戌~丑 / morn=卯辰 / dusk=酉戌）；赌馆入夜（戌时）出「豪赌一场」（±30 两、<20 两拒客），香烛店清晨（卯时）才可「请香烛」（得线香） | `index.html`（`bldActOk()`/`bldActsFilter()`/赌馆·香烛 acts）、`shared/data/items.js`（xiang 线香） |
| **城市系统** | ✅ 完成 | 程序化城市（人口/秩序/商业驱动内容）、城市网格地图（NxN 房间、点击移动、cell 派生 NPC）、城市兴衰 + 废墟火灾 + 阵营占领、道路分层（泥路→官道，火破坏道路/烧城门削弱守军，可破路断路，自动寻路到城门）、城市参数（农业/城型/城门/方位/交易品）、城市名称标准化 + 商铺招牌程序生成 + 城市参数面板 | `index.html`（城市网格/兴衰/占领/道路/招牌）、`shared/data/markets.js`、`shared/story/rooms.js`（城市房间） |
| **可进入建筑（interior 机制）** | ✅ 完成 | 店铺 / 玩家营造建筑成为可步入的真实房间（动态 `__bld__` 房间 + 子区域导航 + 返回街道）；建筑 interior 放置物件渲染（商店里帐篷不再消失）；build.js 回调全局桥接（铁砧打造面板）；城市网格按单元格隔离放置 | `index.html`（`enter_building`/`renderRoom` interior 分支）、`shared/data/build.js`（interior 字段）、`shared/story/rooms.js` |
| **山河志·地图大重构** | ✅ 完成 | 地图底图改为参考图 PNG 视觉主体（隐藏 SVG 遮罩）；列表式州图 → 迷你 13 州 svg 缩略图联动 → 三国英雄传风格 canvas（散布节点+道路线）→ 河流 + zoom/pan + 掉落图标/图例/教程；拉远时显 SVG 州界/州名；全部 13 州治房间 | `index.html`（`buildMapHTML`/`initMap`）、`assets/_pv_ref.png`、`shared/story/rooms.js`（13 州治） |

---

## 二、已落地系统详解

### 2.1 战斗系统（P0）
- **伤害公式（百分比减伤模型，2026-08-18 改）**：`减伤率 = min(0.8, 防御战力 × 1%)`，`伤害 = 攻 × 伤害系数 × (1 - 减伤率)`，保留 20% 穿透避免 softlock。
  - 旧式 `攻×100/(100+防)` 为分数递减公式：防御边际收益随 def 平方衰减、攻防比随 def 飙升（数值膨胀到几百几千时双方互相刮痧 → softlock），且攻击约 6 倍占优于防御。
  - 现改百分比模型：每点防御战力恒定 1% 减伤（配合 `ATTR_RATIO.def ×4` → 每点防御属性≈4%减伤），溢出点从 def32 升至 def80，当前数值全程有效；减伤率有 80% 硬上限，def 再高也不崩。
  - 参数：`DEF_DR_PER=0.01`、`DEF_DR_CAP=0.80`、`DMG_SCALE=1.85`（落点 `shared/combat/engine.js`）。
- **节拍（beat）**：每招式带 `beat` 出招耗时，速度/轻功降低节拍、触发额外行动；重招节拍长、轻招可连击。
- **战意槽**：普攻/受击攒战意，满槽放绝技（`type==='ultimate'`）。
- **4 种 AI**：`aggressive`（猛攻）/ `defensive`（守势）/ `balancer`（均衡）/ `boss`（`hua_xiong` 专用，带阶段文本）。
- **半手动战斗**：每回合等待玩家指令；自动战斗已移除，节奏由文字演出速度档调节。
- **修复记录**：战斗引导/教学链若干 bug（lint 全程 0 错误）。

### 2.2 武学合一（P2 最小版）
- **真招式 = 真出招**：`MARTIAL_ARTS` 既用于研习也用于战斗，消除旧 `G.SKILLS`（纯数值加成）与战斗脱节的「假武学」。
- **13 艺线分组**：`fist/sword/blade/spear/staff/halberd/hammer/whip/bow/hidden/ride/light/internal`，研习界面按线分区展示。
- **门槛锁**：`learn.lineMin` 要求对应艺线等级；学一招该线 +1 级，玩家自然爬树解锁高阶招。
- **发力技巧层**：`type==='technique'`（如寸劲），可脱离本武学装配任意招式，行囊带 `◆` 金标。

### 2.3 资源硬性限制
- **食/水**：随行走、时辰流失；耗尽则 `maybeStarve()` 在每次行动/战斗结算侵蚀气血（食尽 -6、水尽 -4，双尽 -10，下限保 1）。
- **精力**：移动 -4、应战 -5；`exert()` 守卫在移动/市集/巡山/赴洛阳/应战前检查，≤0 则封锁并提示「须先休整」。
- **防卡死**：城/林/溪三处均补「休整」入口，力竭后任何房间都能恢复。
- **状态栏警告**：精力耗尽显示红色「⚠ 精力耗尽·无法行动」；食/水为 0 数值标红并提示「饥馁缠身」。

### 2.4 主线门控（P4）
- 斩华雄后 `state.quest.luoyang=true`，营中「赴洛阳」入口解锁，新增洛阳城房间（朱雀大街凯旋剧情 + 休整）。
- 任务弹窗展示：剿匪计数、斩华雄状态、洛阳解锁状态、真实声望称号。

### 2.5 设置面板（标签页重构）
- 底部 dock「更多」改为「设置」按钮，打开标签页式设置面板（**画面 / 声音 / 游戏**）。
- **画面**：文字演出速度（疾/常/缓，原「整肃行装」项）、标题特效开关（开关水墨烟尘/墨晕动画，关则隐藏特效层）。
- **声音**：音效开关（开则互动时有轻柔提示音，Web Audio 现场合成，默认关）。
- **游戏**：立即存档、清除全部存档、调试台。
- 标题页「整肃」入口打开同款标签页，但通过 `openModal('settings',{fromTitle:true})` 标记**不显示调试台与立即存档**（游戏内才有），仅保留清除存档；设置项与游戏内共用同一套 `settings`。
- 新增 `applyTitleFx()`（标题特效关时隐藏特效层）与 `tick()` 提示音（toast 与各项切换时触发）。

### 2.6 内容 / 视觉微调
- **清风茶肆·说书先生**：不再讲「温酒斩华雄」（三国事在光和元年尚未发生，设定不符），改为讲先秦游侠（豫让漆身吞炭、朱亥袖椎）、北地燕赵豪侠、汉末党锢风云、涿郡草莽；茶肆场景描述与细看文案同步去「三国纷争/温酒斩将」。
- **视觉**：随机不规则墨晕（去除位移与旋转、调淡去脏、极缓生长淡出 ease-in-out 14–20s、更稀更柔）；底部行军尘烟动效。
- 游戏其余三国味（标题「三国群侠传」、华雄 Boss 战）保留；其中 Boss 战名「温酒斩华雄」因系关羽 190 年典故（光和元年尚未发生），已改为时代中立的「力斩华雄」，去除「温酒斩将」典故，与当前时间锚点一致。

---

## 三、代码落点索引（维护用）

| 文件 | 职责 |
|------|------|
| `index.html` | H5 主端：UI 渲染、状态栏、研习/行囊/任务/更多弹窗、`handleAction` 调度、战斗界面、资源逻辑（`advanceTime`/`maybeStarve`/`exert`） |
| `shared/index.js` | `defaultSave()`（存档结构，开局 `sect:null` 无门派）、`normalize()`（旧档兼容）、`applySect()`（新建/读档时以基础值套用已记录的门派）、`canJoinSect()`/`joinSect()`（中后期主动加入门派，增量叠加、保留养成、可转投） |
| `shared/combat/engine.js` | 战斗引擎：初始化、回合、出手、Buff、AI、结算 |
| `shared/data/martial.js` | `MARTIAL_ARTS`：招式/绝技/发力技巧 + 13 艺线定义 |
| `shared/data/enemies.js` | 敌人模板（含声望奖励隐含映射见 `index.html` `RREP`） |
| `shared/data/items.js` | 物品定义（含建材 `shitiao/zhuan/tiekuai`、图纸 `tuzhi_yeolian` 等）+ `makeItem` 生成 |
| `shared/data/recipes.js` | 工作台配方（木工台：木材/器具/建筑类，含「窑烧为砖」） |
| `shared/data/build.js` | **营造系统蓝图数据 `LF.BUILD`**（图纸→工地→多阶段→落成，当前含冶炼工坊） |
| `shared/data/shops.js` | 商店数据（货郎/药铺） |
| `shared/data/rooms.js` → 实为 `shared/story/rooms.js` | 房间/出口/NPC/actions（营/城/林/溪/洛阳） |
| `shared/config/constants.js` | `GAME_NAME`、`VERSION`、`MAX_LEVEL` |
| `test/build_system_test.js` | 营造系统数据层自测（蓝图结构/图纸映射/配方/材料） |
| `GAME_DESIGN.md` | 设计基线 v2.2 |
| `PROJECT_GUIDE.md` | 三端统一规范（主端 H5） |

> ⚠ 注意：设计文档 §3.1 计划的结构为 `shared/story/*`、`shared/config/*`，实际落点为 `shared/data/*`、`shared/story/rooms.js` 混用，与计划略有出入但功能自洽，可后续规范化。

---

## 四、存档兼容

- `defaultSave()` 含完整字段；旧档经 `normalize()` 自动补 `quest`、三资源、艺线键等，升级无痛。
- 调试台仅在 URL 带 `?dev=1` 时出现（如 `http://localhost:8123/index.html?dev=1`），常规玩家不可见。

---

## 五、下一步建议（按优先级）

| 优先级 | 项 | 说明 | 依赖 |
|--------|----|------|------|
| ✅ 已完成 | 装备系统（P1 完整） | 四槽+品质+耐久，掉落生成并接入战斗属性（详见路线图表） | — |
| ✅ 已完成 | P2 完整版 | 艺线经验由战斗累积+学招、艺线等级→攻/命/暴/内/速、境界突破效果接入、命中判定系统（详见路线图表） | — |
| ✅ 已完成 | P3 善恶双轨 | 侠义/凶名独立双轴 + 阈值解锁（侠义委托/影门悬赏/枭雄隐藏线）+ NPC 态度系统（详见路线图表） | — |
| 🟢 | P5 影门 / P6 家族 | 仅骨架，规则待细化 | 阵营/声望系统 |
| 🟢 | 放置内核 | 已移除（纯文字 RPG 路线，不再含离线/挂机收益） | — |

---

## 六、当前可玩内容速览（玩点 + 流程）

### 6.1 游戏状态（v0.2.0）
- 框架闭环自洽，**61 个场景房间**（颍川主营·颍川城·北邙山林·溪畔草庐·洛阳 + 蓟城诸坊[通衢/南门/北门/州牧/学宫/大市/酒楼/军营/练武场/茶肆/平民里/民居等] + 黑山寨 + 北疆边塞[渔阳12坊·白檀军屯6·燕山6]）；开局新增**捏人界面**（自输姓名 + 选出身禀赋 + 六维先天资质点配 15 点，基础 5 / 范围 1–10）与**序章开场叙事**（光和元年背景打字机演出），新档位点击即进入捏人→序章→入局
- 时间：年号「**光和元年**（公元 178）」，农历 + 公历双轨 + 12 时辰，状态栏常驻古历 + 天候
- 战斗：12 招式数据（9 招式 + 3 发力技巧，含绝技回马枪）+ 10 敌模板 + 4 AI + 华雄 Boss；半手动
- 武学：13 艺线真招式研习 + 发力技巧装配，研习界面带门槛锁
- 声望：8 档称号，华雄门控声望≥20
- 善恶：侠义/凶名独立双轴 + 阈值解锁 + NPC 态度系统
- 主线：剿匪 → 斩华雄 → 解锁洛阳凯旋
- 三资源硬性限制（食 / 水 / 精力），力竭封锁、休整恢复
- 背包：掉落堆叠入库、行囊展示（装备用途接入 P1 完整）
- **营造（生存建造 MVP）**：图纸（如《冶炼工坊图》）→ 房内工地 → 填充建材（石料/砖头/木材）→ 分阶搭建 → 落成新交互建筑（冶炼工坊可「熔石取铁」）；配套采石崖 / 残破木箱 / 货郎收售
- **文字图标 UI**：物品图标全面替换 emoji 为文字排版（单字整字 / 二字一行 / 三字 2+1 / 四字 2+2 两排 / 五字及以上按词典取前 2/3 字成词前缀 + 缩小后缀），覆盖背包 / 商店 / 制作 / 建造 / 详情面板
- UI：场景对象卡片式交互；NPC/物件点击弹出**光标处右键式浮层菜单**（无描述、紧凑）
- 战败封锁：仅留「休整」，战斗按钮彻底清掉、引擎阻断

### 6.2 核心玩点
- 研习武学爬 13 艺线树，门槛锁引导解锁高阶招
- 剿匪 / 巡山 / 治安攒声望，冲击华雄 Boss
- 三资源生存管理，合理「休整」防卡死
- 收集掉落物（入背包，装备系统上线后真正用武之地）
- 手动战斗体验打磨（节奏 / 引导 / 演出）
- 历法/天候营造沉浸；NPC 自由交互（交谈/观察/攻击，强调自由度）

### 6.3 主线流程
燕山·山口（北疆幽州·出生） → 颍川早期可玩区域（剿匪/巡山攒声望，地图系统测试/演示） → 声望≥20 → 挑战台·力斩华雄 → 解锁洛阳 → 赴洛阳凯旋

### 6.4 探索 / 副线
- 颍川城：逛市集交易、维持治安、与华老交谈
- 北邙山林：巡山剿匪、林间栖身
- 溪畔草庐：访水镜先生与左慈、草庐借宿
- 建造系统测试房（北邙山林南）：砍树/采石 → 木工台制材 → 翻木箱得图纸 → 依图营造 → 冶炼工坊熔石取铁
- 北疆边塞（汉末真实地理，蓟城东北→北）：渔阳（北疆门户·12坊：郭门/南门/通衢/北门/西市街/县衙/军营/驿馆/东市街/大市/望蓟楼/听涛茶肆）→ 白檀军屯（滦水屯田戍堡·营门/屯寨/屯田/马苑/粮廪/哨垒，曹操北征乌桓所经）→ 燕山（山脉屏障·山口/峰峦/深涧/林道/古栈道/雪岭，古栈道出塞）；当前**出生点暂设燕山·山口**

---

*最后更新：2026-07-29（**记录当前真实版本**：① 修正文档严重过时的「5 个场景房间」为真实 61 个房间（含蓟城诸坊、黑山寨与新增北疆边塞）；② 设置面板改为标签页（画面/声音/游戏），底部 dock「更多」→「设置」，标题页同款但无调试台；③ 清风茶肆说书先生改讲先秦游侠/北地豪侠/汉末风云（去掉温酒斩华雄，因光和元年三国未起），游戏其余三国味保留；④ 视觉：随机不规则墨晕 + 底部行军尘烟；⑤ 历法/天候/山河志空间地图/NPC 浮层菜单/善恶双轨/装备系统/武学完整版/UI 卡片交互/战败封锁 等均已于此前落地；⑥ **新增北疆边塞地图**：按汉末真实地理校准——渔阳郡治在蓟城东北（今密云），渔阳以北为白檀军屯（滦水屯田戍堡，后世曹操北征乌桓所经）、再北即燕山山脉屏障（汉疆与塞外分界）；洛阳→蓟城→马市北出→渔阳→北门→白檀→燕山→古栈道出塞，构成完整边塞走廊；⑦ **玩家出生点暂设燕山·山口**（`yanshan_shankou`）。当前真实落地规模：**61 房间 / 43 对话对象 / 12 招式数据 / 10 敌模板 / 8 事件 / 13 艺线 / 4 AI / 华雄 Boss**）*

---

## 七、近期背包系统重构（v0.7 → v0.8，2026-08-17）

### 7.1 已落地的背包/装备改动
- **装备槽扩展为 7 个**：原六槽（帽笠 / 衣甲 / 鞋履 / 兵刃 / 饰品 / 腰带）新增独立「背包」槽（`bag`，图标 👝），挂在人形右侧肩膀旁（虚线金边，区别于六装备槽）。
- **动态背包容量 `packMax()`**：基础容量 `BASE_PACK = 6`，并汇总**所有装备槽**上 `packSpace` 字段——腰带槽的腰包、背包槽的鞶囊都计入扩容。
- **装备/卸下后真实重建行囊数组 `packResize()`**：此前「扩容」只改了显示数字、物品格始终 6 个（假扩容）；现每次装备变动后按 `packMax()` 真正增长/压缩 `state.pack` 数组，格子数与容量一致。
- **新增两个背包物品（开局即拥有）**：
  - `便携腰包`（白，占「腰带」槽，`packSpace +4`，描述：软皮小囊系于腰间）
  - `虎头鞶囊`（绿，占「背包」槽，`packSpace +20`，描述：革坚囊阔可容二十）
  - 两物可同时装备，满配容量 = 6 + 4 + 20 = **30**。
- **UI 优化**：标题「行 囊」改亮金 `#ffd268` + 辉光，去掉右侧「装备 · 物品」副词；详情面板最大高度 30%→22% 并全面收紧内边距/字号/间距；详情「容量」行显示「背包空间 +N」；物品详情去除无意义的「此物无损耗」行。
- **容量显示**：底部由「容量 N」改为「容量 当前 / 上限」（如装备虎头鞶囊后显示 `5 / 26`）。

### 7.2 已修复的 bug
1. **装备提示被背包 UI 遮挡**：`#toast` 层级 `z-index:60` 低于背包弹窗 `#modal:70`。已提至 `200`，装备武器/帽子时的提示现在显示在背包 UI 之上。
2. **容量边界不一致**：卸下提供容量的装备（腰包/鞶囊）时，若行囊内物品数已超「卸下后容量」，会进入「显示 X/6 却渲染 26 格」的不一致态。`unequipToPack()` 现已在卸下前校验 `packMax() - eq.packSpace` 是否够装，不够则拦截并提示，杜绝丢物/状态错乱。

### 7.3 玩法改进分析（待办 / 建议，按价值排序）

**A. 扩容取舍张力虽好，但「腰包占腰带槽」削弱防御位**
- 现状：`便携腰包` 占「腰带」槽，意味着穿腰包就**不能穿护腰类防具**（腰带槽本可放加防御的装备）。这是有意取舍，但当前腰带槽除腰包外几乎没有其他「加防御」的装备，导致「要容量还是要防御」实际无得选。
- 建议：① 为腰带槽补 1–2 件「防御型腰带」（如「熟铜护腰」+def），让取舍真实成立；② 或把 `便携腰包` 改为**不占腰带槽、直接作为消耗型/常驻扩容**（如开局自带 +4，无需装备）。优先做①。

**B. 初始容量 6 过小，捡 2–3 件即满，前期挫败**
- 用户明确要 6，保留。但应在**前 10 分钟流程内**确保玩家能轻松拿到扩容手段（腰包/鞶囊已开局拥有，已缓解）。建议：在「营中/市集」增加可购买的扩容道具（如「粗布搭膊」+8，铜钱购买），让容量成长有节奏感。

**C. 物资循环薄弱：只有「用」没有「造」—— ✅ 已于 v20260821j 实装**
- 现状（v20260821j 前）：金疮药/肉包子靠掉落，没有合成/制作。药耗尽只能等掉落，生存压力易卡。
- 已落地：**炊事灶（`kitchen`）配方台**——金疮药 = 草药×2（疗外伤 50）、肉包子 = 生肉×1（食+20/饮+5）；生肉由山贼/野犬掉落、草药由草丛采集；所有休整面板加「炊制·调制」按钮。已打通"采集/掉落素材 → 合成"闭环，强化三资源（食/水/精力）管理。

**D. 背包槽仅 1 个，且被鞶囊独占，策略深度有限**
- 现状：背包槽只接受 `slot:'bag'` 物品，目前仅有 `虎头鞶囊` 一件。
- 建议：补充 1–2 件不同档位的背包（如「粗麻布袋 +10」「锦缎行囊 +16」），让「背包槽」也有多选权衡（容量 vs 品质/重量），与腰带槽形成双轴取舍。

**E. 新手上手指引不足**
- 现状：「背包槽/腰带槽」区分、`packSpace` 扩容机制对玩家不直观，详情面板虽有「背包空间 +N」，但没说明「需装备于背包/腰带槽才生效」。
- 建议：① 在装备物品的详情「部位」行后追加一行小字「（装备于对应槽位即扩充行囊）」；② 首次打开行囊时，对空「背包」槽做一次性高亮提示「拖入鞶囊可扩容」。

**F. 数值/平衡（轻量）**
- `虎头鞶囊 +20` 一步到位到满配 30，前期即封顶，后期容量成长失去意义。建议中后期用「更重的代价 / 更高阶但需条件解锁」的背包（如「储物锦囊 +30 但占 2 槽位」），维持成长线。

> 优先级建议：先做 **A① + E**（低风险、直接提升体验与可理解性），其次 **C + D**（内容厚度），最后 **B/F**（平衡微调）。

### 7.4 本轮新增内容（2026-08-17 续）

**背包 / 腰带物品扩充（均接入 `packSpace` 扩容机制）**
| 物品 | 槽位 | 扩容 | 附加属性 | 品质 | 获取 |
|---|---|---|---|---|---|
| 小囊 | 背包 | +6 | — | 白 | 行商购（银10） |
| 书囊 | 背包 | +10 | 悟性 +1 | 绿 | 行商购（银18） |
| 简易行装 | 背包 | +4 | 身法 +1 | 白 | 行商购（银8） |
| 皮革包裹 | 背包 | +14 | — | 绿 | 行商购（银22） |
| 采药背篓 | 背包 | +15 | — | 绿 | 行商购（银24） |
| 熟铜护腰 | 腰带 | — | 防御 +1 | 白 | 行商购（银12） |

- **悟性首次接入装备属性**：装备 `书囊` 时通过 `applyEquipApt()` 把 `wuxing` 增量同步进 `state.apt.wuxing`（卸下回退）；详情「属性」区与 `statText` 均显示「悟性 +1」。至此背包槽形成「容量 vs 悟性/身法」双轴权衡。
- **腰带槽取舍真正成立**：`熟铜护腰`(+1防) 与 `便携腰包`(+4容量) 互斥，玩家须二选一。

**野外采药点（北邙山林·草药丛）**
- 北邙山林（`forest`）新增 `草药丛` 物件：点「采集草药」→ 耗时 1 时辰 → 按钮变为「拾取草药」；点「拾取草药」得草药 2~3 株入包。
- 背包满时拾取会提示：「行囊已满，草药散落于地，无处安放——腾出空位再来拾取。」
- 每日每地限采 3 回（`state.gatherCount[room@day]`），草药可作后续合成素材。
- 物件 `actions` 支持函数式，按 `state.gather` 状态动态切换按钮（采集 / 拾取）。

**Fix：卸下回退资质的时序 bug**
- `unequipToPack()` 原把 `applyEquipApt(eq,-1)` 放在容量拦截 `return` 之前；若拦截触发，悟性已被扣但装备未卸下，导致资质永久丢失。已将其移至拦截通过之后。

**其它**
- 起始银两 50 → 200（便于测试购买背包等道具）。
- `applyEffect` 新增 `give` 字段（数组 `{defId,n}`），支持事件/行商发放物品；满则提示未得。
- 行商（`ev_merchant`）新增 6 项可购背包/腰带。

### 7.5 装备栏布局修复（2026-08-17 续）

**问题**：在 124×130 的 `.equip-figure` 容器里塞了 6 装备槽 + 1 背包槽，坐标互相挤压：`weapon`（left:78 top:55 w:30）与 `cloth`（left:38 top:51 w:48）水平/垂直重叠约 8px，`bagflow`（left:86 top:24 w:38）与 `trinket`（left:38 top:27 w:48）右边缘紧贴；长装备名（如「皮革包裹」「采药背篓」4 字）超过 38px 槽宽时无 `ellipsis` 截断，会溢出覆盖到相邻槽位，导致「字都叠在一起看不清」。

**修复**：
- 重新规划为整齐 2 列网格：`hat`/`trinket`（行1）→ `cloth`/`weapon`（行2）→ `belt`/`bagflow`（行3）→ `shoe`（行4 占满）。每个槽宽度提升到 56~58px，互不重叠。
- `.ep-name` / `.ep-ph` 加 `white-space:nowrap; text-overflow:ellipsis`，单行超长自动省略。
- `.equip-figure` 高 130→104，配合 SVG 装饰透明度 0.35（人形只作背景，不参与槽位定位）。
- `.ep-bagflow` 字号 10.5px，4 字名可完整装入 58px 槽。

---

## 八、战斗平衡优化与界面打磨（2026-08-18）

### 8.1 伤害公式重构（抗数值膨胀）
- **动因**：旧分数公式 `攻×100/(100+防)` 在防御堆高时边际收益平方衰减、攻防比随 def 飙升，数值膨胀到几百几千会 softlock；且攻击约 6 倍占优于防御，防御形同虚设。
- **新模型**：百分比减伤 `减伤率 = min(DEF_DR_CAP, 防御战力 × DEF_DR_PER)`，`伤害 = 攻 × DMG_SCALE × (1 - 减伤率)`。
- **参数**（`shared/combat/engine.js`）：`DEF_DR_PER=0.01`、`DEF_DR_CAP=0.80`（保留 20% 穿透，不 softlock）、`DMG_SCALE=1.85`（保持开局手感：玩家打怪≈26、怪打玩家≈22）。
- **防御换算**：`shared/index.js` `ATTR_RATIO.def` ×2 → **×4**，每点防御属性≈4% 减伤；溢出点从 def32 升至 def80，当前数值（玩家≤80/怪≤45）全程有效。
- **平衡效果**：攻防比从 ~6 收敛到 ~3.3；华雄 def45 减伤从 80% 溢出回落到 45%，Boss 战时长缩短约 2.3 倍；攻防取舍出现（高攻敌堆防划算、低攻敌堆攻划算）。
- 测试副本 `_t.js` 同步。

### 8.2 界面打磨
- **角色面板**：移除「门派」行（尚未实装，临时隐藏）；「等级」下方新增**修为经验条**（文字 `修为 X / Y` + 金色百分比进度条，满级显示「圆满」并填满）。
- **银两移位**：角色面板移除「银两」行，改为在**背包底部**显示「银两 N」（金色高亮），货币一眼可见。
- **战斗 HUD**：玩家铭牌去除门派，改为「LV.x · 游侠」。
- 落地：`index.html`（`openChar`/`renderPack`/战斗 HUD `renderCombat`），新增 CSS `.exp-bar` / `.pack-gold`。

### 8.3 捏人点数不再白丢（bug 修复）
- 旧逻辑 `confirmCreate` 把 `freePoints` 写死为 0，捏人未分配的 5 点直接丢失。
- 现改为 `save.freePoints = createState.pool`，未分配点自动转成入局后可分配的「自由属性点」，玩家随时可在角色面板补点。

### 8.4 代码清理
- 删除 `shared/index.js` 中未使用的死常量 `BASE`（从未被读取，且与 `defaultSave` 字面量重复，易误导维护者）；修正 `defaultSave.freePoints` 过时注释与初值。

---

## 九、营造系统 MVP + 文字图标 UI（2026-08-21）

### 9.1 营造（生存建造）核心闭环
- **数据层**：新增 `shared/data/build.js`（`LF.BUILD` 蓝图表），蓝图定义 `key/siteIcon/siteName/doneIcon/doneName/stages/done`——`stages` 为多阶段建造（每阶段含 `name` 与 `need` 材料表），`done` 标识落成后的功能动作。首张图：**冶炼工坊**（两阶段：夯筑石基→立木架炉）。
- **物品**：新增建材 `shitiao`（石料）/`zhuan`（砖头）/`tiekuai`（铁料）与图纸物品 `tuzhi_yeolian`（冶炼工坊图，`cat:'图纸'`）；`makeItem` 透传 `blueprint` 字段；木工台新增「窑烧为砖」配方（石料×2→砖头×1）。
- **玩法链路**（建造测试房·北邙山林南）：
  1. 残破木箱翻出《冶炼工坊图》；
  2. 采石崖采石料（每日限 3 次，同采药）、老树伐木、木工台制木材/烧砖；
  3. 背包选图纸 →「依图营造」→ 房间出现「冶炼工坊（营造中）」工地；
  4. 工地「填充材料」（面板逐料填充，反馈显示在面板内提示条）→「搭建」（当前阶材料齐则推进）→ 两阶完成落成；
  5. 落成后点工坊「炉膛」：投铁矿石+木材 → 点火烧制 → 随时辰推进出进度条 → 收取铁料（生产链起点）。
- **存档**：工地/建筑存于 `state.placed`（`{key, defId, bp, stage, got, done}`），与普通放置物同源、可随档保存。
- **修复**：① 放置物品只扣 1 个而非整格清空（堆叠帐篷/图纸不再一起消失）；② 填充反馈改显在建造面板内，不再被弹窗遮挡。
- 测试：新增 `test/build_system_test.js`（24 断言：蓝图结构/图纸映射/配方/材料定义），内联脚本语法检查 0 错误，战斗回归 48/48 通过。版本 bump `20260821e → 20260821f`，缓存参数同步。

### 9.2 文字图标 UI（替代 emoji 物品图标）
- **动机**：emoji 风格不统一、品类覆盖不足，且部分图标语义不贴合。
- **规则**（`itemIconHTML`/`iconParts`）：单字=整字；二字=一行；三字=前 2 大 + 后 1 小；四字=上下两排 2+2 等大；五字及以上=优先取前 2/3 字能成词的前缀大字，其余缩小（内置约 70 词词典 `ICON_WORDS`，可扩充）。
- **覆盖范围**：背包格子、商店货品/行囊栏、制作面板（材料+产出）、建造面板、物品详情、买入面板、场景放置物/工地/工坊图标。
- 场景环境元素（树/木箱/采石崖等）与装备、NPC 保留原样式（非物品类）。

### 9.3 后续迭代（2026-08-21，v20260821g/h/i）

**g · 跳教程直达建造测试场 + 出生点**
- 捏人界面勾「跳过新手教程」后出生地为建造测试房 `build_test`（原为黑山寨），教学标记照常完成。

**h · P0 打磨：让建造闭环"转起来"**
- 铁料接入用途：新增铁器（铁剑/铁斧/铁马掌）与 `forge` 工作台配方；冶炼工坊落成后新增「打造…」；铁斧使伐木/采石产量 ×2（耗耐久）。
- 图纸多渠道：货郎出售《冶炼工坊图》+ 建材；山贼/流寇头目掉落石料/木头/砖头。
- 建造耗代价：填充/搭建/熔炼均耗时辰+精力，精力不足拦截；掉落入库统一走 `makeItem`。
- 测试：build_system_test 扩至 36 断言（forge 配方/铁器产出/货郎可购/敌人掉落）。

**i · 场景文本精简 + 背包 icon 留白 + 自由休息 + 炉膛冶炼**
- 场景入场描述精简为短句（`shortScene`，首句截断≤28字），完整描述保留在「探查/环顾」。
- 背包/商店行囊格：文字 icon 缩至 13px、去掉名字行、格子加 padding 四周留白，不再溢出。
- 休息系统重构：**移除 dock 调息**；帐篷/草席/篝火改「休息…」弹自由时长面板（小憩 1 / 安睡 3 / 酣眠 6 时辰，随时长线性恢复+推时辰+耗食水）；设施效率分级（帐篷>草席>篝火>席地打盹）；战败封锁改为场景内「席地打盹」入口。
- 熔炉冶炼：新增铁矿石物品；采石崖 35% 兼得（铁斧翻倍）、货郎出售、山贼掉落；炉膛面板投铁矿石+木材→点火→进度条随时辰推进（`advanceTime` hook `tickForge`）→收取铁料；燃料耗尽熄火进度保留。

---

### 9.4 物资合成 + 主线收尾钩子（v20260821j）

**物资合成（打通生存闭环，回应 §7.3 C）**
- `items.js`：新增素材 **生肉**（猎获/劫掠所得）；`enemies.js`：**山贼**、**野犬**掉落加入生肉（野犬为新手练级敌，便于早期稳定获取）。
- `recipes.js`：新增 **`kitchen`（炊事灶）** 配方台——金疮药 = 草药×2（疗外伤 50）、肉包子 = 生肉×1（食+20/饮+5）。
- `index.html`：所有**休整面板**加「炊制·调制」按钮打开炊事灶；制作面板标题随台变化（🍳 炊事灶）。此前金疮药/肉包子只靠掉落、无制造、易卡死，现已打通"采集/掉落素材 → 合成"闭环。

**主线第一章收尾钩子**
- `events.js`：新增 **`ev_kaixuan_decree`（凯旋·关东盟檄）**——洛阳贺宴后传关东诸侯联署讨董盟檄，给三条第二章走向（受檄·讨董联军线 / 游历·开放世界线 / 寻师·门派线 / 辞谢），设 `ch2_*` 旗标。
- `triggers.js`：新增 **`kaixuan_hook`**（`once`，凯旋后首访洛阳自动触发），铺陈第二章。
- `VERSION` → `20260821j`，同步 6 个 `?v=` 缓存参数；清理野犬掉落误留的 `time:0` 字段。

### 9.5 UI 优化（v20260821k）
- **物品图标统一**：`itemIconHTML` 不再按字数拆分/缩小，直接显示物品全称、统一字号（移除 `iconParts`/`ICON_WORDS` 词表逻辑）；长名称允许在格内换行。配套删除制作/货郎/背包详情等处与图标重复的 `name` 文本拼接。
- **货郎售价遮挡修复**：`.sg-buy` 由绝对定位（覆盖商品名）改为名字下方独立标签；移除格子内与商品名重复的图标元素。
- **任务栏优化**：移除"当前所处"（玩家位置属角色面板，不应在任务栏）及"已习武学/发力技巧/江湖声望"等角色属性行，仅保留 4 项任务（剿匪/斩华雄/洛阳/送信），完成态着色（绿=达成、灰=未达成）。
- `VERSION` → `20260821k`，同步 `constants.js` 的 `?v=` 缓存参数。

### 9.6 UI 修复 + 地图底图接入（v20260821l）
- **货郎溢出**：移除货品格内重复的 22px 图标，保留 11px 商品名 + 独立售价。
- **背包角标**：`.pcell-ic` 底部加 padding 上移；角标统一 `z-index:3`，避免被图标遮挡。
- **地图底图**：`map_ai.png` 实为集市插画（生图跑偏），废弃；启用 `assets/map_parchment.png`（羊皮纸底纹），与上层州界/河流/节点叠加得"山河志"质感。
- **"回到当前位置"按钮**：地图右下角浮动，点击平移到当前所在（设置出生点模式下隐藏）。
- `VERSION` → `20260821l`。

### 9.7 UI 修复（背包/货郎字溢出 + 地图州名归位，v20260821m）
- **背包/货郎字溢出**：`.ic-txt` 改为横排单行 + 超宽截断；删旧拆分样式；货郎 `.sg-name` 占满宽截断、`.shop-good` 加 `min-width:0`。
- **背包角标**：`.pcell-ic` 底部留白增至 15px，避开数量/售价/耐久角标。
- **地图州名重叠**：兖 `[10,7]→[0,0]`、豫 `[6,8]→[0,1]`、冀 `[9,3]→[0,0]`，归位到州内（label 单位为网格，原偏移过大推飞）。
- **地图美化**：州名加描边。
- `VERSION` → `20260821m`，同步 `map.js` 缓存参数。

### 9.8 UI 修复（背包/货郎字完整显示 + 地图全览去重，v20260821n）
- **背包/货郎字省略号**：改横排换行完整显示（去 nowrap/ellipsis）。
- **背包留白**：`.pcell-ic` 四周留白、底部 16px 给堆叠数字让位。
- **地图州名重叠**：`lv0` 全览隐藏州名（只留轮廓）、`lv1` .85、`lv2` .42 淡显，彻底消除全览挤字。
- `VERSION` → `20260821n`。

### 9.9 UI 修复（选中光圈 + 货郎长名/超高售价自适应，v20260821o）
- **选中光圈**：`.pcell-sel/.pcell-insp` 改 `inset` + 5px 光晕，框内高亮不外溢。
- **货郎长名**：`sgFontSize(name)` 按字数降级（≤2/3/4/5/≥6 → 12/11/10/9/8.5px）；`word-break:keep-all` 防逐字换行。
- **货郎超高售价**：`fmtPrice(n)` 万/亿单位自适应。
- `VERSION` → `20260821o`。

### 9.10 紧急修复：货郎面板白屏（v20260821p）
- **根因**：o 版本 `fmtPrice`/`sgFontSize` 插入编辑"成功但磁盘未变"，`renderShopPanel` 调用未定义函数抛 `ReferenceError`，面板白屏。
- **修复**：把两函数真正写入函数体内调用前，lint 0。

### 9.11 UI 微调：货郎售价标签整齐（v20260821q）
- `.sg-buy` 固定 `min-width:46px` + 居中，整列对齐。
- `VERSION` → `20260821q`。

### 9.12 UI 修复（选中高光 + 顶字换色 + 州界描边，v20260821r）
- **选中高光**：`.pcell-sel/.pcell-insp` 改实色描边+内填充+外发光；修 `.equipslot.pcell-sel` 漏改弱版。
- **窗口顶字**：`.shop-title` 黄 `#ffd268` → 亮金白 `#fff3c8` + 加粗。
- **州界描边**：`fill:rgba(120,90,50,.05)` + `stroke:rgba(92,66,38,.55)` 1.6px。
- `VERSION` → `20260821r`。

### 9.13 地图按参考图重做（v20260821s）
- **根因**：13 个州 `label` 偏移过大（部分 +10+ 网格），州名被推离多边形、压住邻州。
- **方案**：`write_to_file` 整体重写 `shared/data/map.js`（poly 数据未动，13 处 label 归位为 |dx|,|dy|≤1 小偏移，落在州内中心）。
- **档位规则**：`lv0` 州名 `.35`、州界 1.6px 描边清晰；`lv1` 州名 `.95`；`lv2` 州名 `.55`。
- `VERSION` → `20260821s`。

### 9.14 UI 修复：选中态去外发光，改纯底色区分（v20260821t）
- **反馈**：背包/货郎选中后高光（外发光 + outline + 半透填充）溢出格子、像贴纸飘在表面，要求改为加深底色即可，不外溢。
- **改动**（`index.html`）：
  - `.pcell-sel / .pcell-insp / .equipslot.pcell-sel / .equipslot.pcell-insp / .ep-bagflow.pcell-insp` 一律去掉 `outline` 与外发光 `box-shadow:0 0 N px`，改为 `background` 升 alpha 至 `.42`（金/青）+ `inset 0 0 0 2px` 同色描边，叠加在 `.packcell` 的 `rgba(0,0,0,.18)` 玻璃底上，对比清晰、不超格。
- `VERSION` → `20260821t`，仅同步 `constants.js` 的 `?v=`（CSS 仅在 `index.html`）。

### 9.15 地图州名挤成一团修复（v20260821u）
- **根因**：州名用 SVG `<text>` 渲染，原代码 `var lc=s.label||...` 把 `label` 当**绝对网格坐标**传入 `px()/py()`；v20260821s 误把 `label` 当"相对质心偏移"改成 `[0,0]/[-1,1]` 等近原点值，导致全部州名被钉在网格原点、挤成一团。
- **改动**（`index.html` `buildMapHTML`）：新增 `polyCentroid(poly)`（shoelace 面积质心，退化回退顶点平均），州名落点 = `质心 + label`（label 保留为相对质心小偏移语义）。州名自然散布各州域中心、互不重叠。
- `VERSION` → `20260821u`，同步 `constants.js` 的 `?v=`。

### 9.16 货郎货品新增选中态（v20260821v）
- **反馈**：行囊选中已是纯底色，但货郎左侧货品点选后毫无区别。
- **根因**：左侧 `.shop-good` 渲染时未给选中类，且点击直接 `showBuyPanel` 弹购入，没有"选中→看信息"的中间态。
- **改动**（`index.html` `renderShopPanel`/`bindShopPanel`/`renderShopInfo`）：
  - 新增状态 `shopGoodSel`；货品格渲染时按 `shopGoodSel===r.id` 加 `pcell-sel` 类（金色加深底 + 内嵌描边，与行囊一致、不溢出）。
  - 货品点击：第一下 = 选中（高亮 + 右侧信息区显示该货品名/描述/「购买」按钮）；**再点同货品** = 弹购入数量面板（`showBuyPanel`，已暴露到 `LFUI`）。
  - `renderShopInfo` 优先显示被选中货品；选行囊时自动取消货品选中，反之亦然。
  - CSS 显式 `.shop-good.pcell-sel{background:rgba(232,184,86,.42);box-shadow:inset 0 0 0 2px rgba(255,215,138,.9);}`。
- `VERSION` → `20260821v`，同步 `constants.js` 的 `?v=`。

### 9.17 地图凉州按参考图重画至西北（v20260821w）
- **依据**：用户提供的东汉十三州参考图（`ScreenShot_20260821_230620_978.png`，可经 `read_file` 读取）→ 确认凉州本在最西北（河西走廊：敦煌/酒泉/张掖/武威），并州在其正东（河东北）、益州在其西南。
- **改动**（`shared/data/map.js`）：重写凉州 `poly` 为西北梯形块（顶点 `[-13,-3],[-14,1],[-13.5,6],[-13,11],[-11.5,12],[-10,11],[-10,7],[-12.5,6],[-12.5,2],[-13.5,-2]`）：东距并州西界（col -8）留黄河空档、南与益州西界（col -12.5）共边，互不重叠；武威 zone 坐标 `[-4,1]→[-10,8]` 迁入凉州域内。
- `VERSION` → `20260821w`，同步 `map.js` 的 `?v=`。

### 9.18 货郎选中报错修复 + 地图改用参考图底图（v20260821x）
- **货郎报错**：点击货品触发 `renderShopInfo`，其中 `fmtPrice(price)` 报 `ReferenceError: fmtPrice is not defined`。
  - **根因**：`fmtPrice`/`sgFontSize` 定义在 `renderShopPanel` **内部**（局部），而 `renderShopInfo` 在 `renderShopPanel` **外部**（独立函数），作用域访问不到。
  - **修复**：将 `fmtPrice`/`sgFontSize` 提升为模块级（位于 `renderShopPanel` 之前），删除 `renderShopPanel` 内部重复定义；`renderShopInfo` 与 `goodsGrid` 共用。
- **地图改用参考图底图**：用户反馈手画多边形"太丑"，要求直接用参考图。
  - `.map-grid` 背景改为 `url('ScreenShot_20260821_230620_978.png') center/100% 100% no-repeat`（参考图铺满）。
  - `.map-state-sh` polygon 改为 `fill/stroke:transparent`（隐藏手画轮廓，仅保留透明热区供点击）。
  - `.map-state-name` 改为米白填充 + 深棕描边（paint-order:stroke），保证在彩色参考图上清晰可读。
  - 各档 `opacity` 调高（lv0 .82 / lv1 .95 / lv2 .7），州名始终可见。
- `VERSION` → `20260821x`，同步 `constants.js` 的 `?v=`。

### 9.19 货郎属性 undefined + 双高亮修复 + 隐藏自加州名（v20260821y）
- **货品点击属性显示 undefined**：
  - **根因**：`renderShopInfo` 货品分支直接读 `r.icon/cat/desc/name`，但 `LF.SHOPS` 的 items 只有 `{id, buy, sell}`，没有这些字段。
  - **修复**：从 `LF.ITEMS[r.id]` 取真实数据（`def.icon/.name/.cat/.desc`），货品名/分类/描述正确显示。
- **双高亮（货品 + 行囊同时亮）**：
  - **根因**：行囊 click（`data-loc`）只清自己的 `.packcell` pcell-sel，没清货品的 `[data-shop]` pcell-sel，也没把 `shopGoodSel` 复位。
  - **修复**：行囊 click 同步清 `[data-shop]` 的 `pcell-sel` + 把 `shopGoodSel` 设为 null（与货品 click 已有的清行囊逻辑对称，形成"互斥单选"）。
- **隐藏自加州名**：
  - **根因**：参考图 PNG 自带隶书大字州名（如"幽州""凉州""冀州"），与我们加的米白描边州名叠成双层字。
  - **修复**：`.map-state-name{opacity:0;}`，参考图自带文字独占显示。
- `VERSION` → `20260821y`，同步 `constants.js` 的 `?v=`。

### 9.20 山河志·地图大重构（v20260821z – v20260822as）
- 地图底图改为**参考图 PNG 视觉主体**（`assets/_pv_ref.png`），隐藏 SVG 遮罩避免双层边界重影；拉远时再显 SVG 州界/州名（v20260822a–ac）。
- 地图形态多轮迭代：列表式州图（v20260822am）→ 迷你 13 州 svg 缩略图联动（v20260822ao）→ 三国英雄传风格 canvas（散布节点 + 道路线，v20260822ap）→ 河流 + zoom/pan + 掉落图标/图例/教程（v20260822ar）→ 更宽河流 + 全部 13 州治房间（v20260822as）。
- 移动端/UI：背包/货郎字溢出、商品网格 `minmax` 防溢出、价格列、选中态自适应、健康游戏公告移到加载页（v20260822aa–af）。

### 9.21 城市系统（v20260823a – v20260825d）
- **程序化城市**：人口/秩序/商业驱动内容（v20260823a）。
- **城市网格地图**：程序化 NxN 房间、点击移动、cell 派生 NPC；网格渲染 + 方向移动条；分层中心（宫殿/官署）、具体店铺、loc-tab 单元格名（v20260824a–c）。
- **城市兴衰 / 废墟火灾 / 阵营占领**：城市可被焚毁、被势力攻占（v20260824d）。
- **道路分层**：泥路→官道；火破坏道路、烧城门削弱守军；可故意破路/断路；自动寻路到城门（v20260824g）。
- **城市参数面板**：农业/城型/城门/方位/交易品；城市名称标准化（方位/交易品/地理/俗语可混可单）+ 商铺招牌程序生成（v20260825d）。

### 9.22 可进入建筑与 interior 机制（v20260824e – v20260825c）
- 店铺 / 玩家营造建筑成为**可步入的真实房间**：动态 `__bld__` 房间 + 子区域导航 + 返回街道（v20260825a）。
- 建筑 interior 放置物件现在渲染（修复商店里帐篷消失，v20260825c）；build.js 回调全局桥接（铁砧打造面板，v20260825b）；城市网格按单元格隔离放置。
- 营造系统由此从"MVP 单一冶炼工坊"演进为"蓝图→工地→搭建→落成→可步入房间 + 功能"的通用机制，且 `interior` 已复用到全店铺。当前玩家可自建蓝图仅 1 张（`bp_yeolian` 冶炼工坊），`build.js` 仍残留 emoji 字段待清理。

### 9.23 UI / 移动端打磨延续（v20260822aa – v20260825f）
- 背包/货郎布局与字体分级、选中高光、健康公告布局、商店左右列（v20260822aa–al）。
- 招牌联动 + 商铺张贴营业时间；修复请柬 / 版本同步；补全历史 changelog（v20260825f）。

### 9.24 当前状态总览与已知缺口（v20260825f）
**系统落点总览（以代码为准）**
- ✅ 完成：战斗 / 武学合一 / 资源限制 / 装备 / 声望 / 善恶双轨 / 历法天候 / NPC 交互 / UI 重构 / 标题屏 / 地图大重构 / 城市系统 / 可进入建筑 / 营造闭环 / 物资合成 / 自由休息 / 炉膛冶炼 / 招牌 / 营业时间。
- ⬜ 仅骨架/未做：P5 影门、P6 家族兴衰、门派/内功（内力 `mp` / `joinSect` 未实装）、GUIDE 引擎（数据驱动 guides.js，开场仍硬编码 `ONB_CHAIN`）、家族时间事件。

**已知缺口 / 技术债（接棒重点）**
1. **文档脱节**：本文档此前停在 v20260821y，本次补录至 v20260825f；`GAME_DESIGN.md` 仍停在 2026-07-29，待同步。
2. **新手教程半迁移（已收口）**：旧燕山 `ys_*` 已清理——`index.html` 中 `ONB_CHAIN` 已移除、`ys_*` 判定改为仅 `camp_*`、`ys_contact` 触发挂起、房间层 `yanshan_*`（6 间）已删除、出生点现为 `camp_yard`、山河志地图燕山节点已移除；`camp_wall` 北出改接 `baitan_tun` 以保证开放世界连通。苦役营 `camp_*` 教程仍仅 3/11 房、5/30 NPC、密道线预览，需补完。
3. **营造 emoji 残留（已清理）**：`build.js` 的 `icon` 字段与 `renderBuildPanel` / 炉膛标题已统一走 `itemIconHTML` 文字图标（v20260825f 前提交）。
4. **无用残留**：`narrative/` 目录为空、`_game_inline.js` / `_del_beggar.js` 未跟踪，疑似调试残留，建议清理或归档。
5. **城市系统全程序生成规划**：除 3 锚点（`camp_*` 苦役营、`ji_heishan_*` 黑山山寨、`build_test` 建造试验地图）外，其余手写 ROOMS 已统一加 `[待程序生成]` 标记，待统一生成器接管；`genCityGrid` 当前仅做单元格内部导航，城市根房间生成器尚未实现。

---

### 9.25 战斗双轨清理 + 运招预告修复（v20260828k）
- **移除旧半手动单敌路径（死代码）**：`engine.js` 删 `playTurn` / `peekEnemyIntent` / `getPlayerActionList` / `getUnitActions`；`index.html` 删旧卡渲染（`renderCombatCard` / `renderCombatRound` / `updateCardBars` / `currentEnemyIntent` / `combatCardEl`）、旧战斗菜单（`openAttackMenu` / `openPackInCombat` / `useItemInCombat` / `openSettingsInCombat` / `addActionBtn`）、`handleCombatAction`（非教学分支）/ `handleCombatFlee` / `combatBusy`。战斗统一 DQ 队伍回合制。
- **运招抢攻预告不一致 bug 消除**：该 bug（beat>60 时敌先抢攻一次再打预告招、UI 只预告主招导致误导决策）位于旧 `playTurn`；删除后 DQ 路径 `e.intent` 锁定「预告=结算」，不再误导。
- **教学战并入 DQ 菜单**：新增 `tutStep()` 状态机（attack→defend→item→finish）+ `dqRenderCommands` 分步高亮/引导文案 + 攻/防按钮接 `tutCombatAct` 演出 + 道具按钮（老乞丐赠行囊前隐藏）+ 撤退拦截；`tutCombatAct` 改操作 `st.enemies[0]`/`st.playerUnits[0]`，刷新走 `dqRenderCard()`/`dqRenderRound()`（顺带修掉教学战「DQ 卡 + 旧卡并存」的隐性双卡 bug）。
- **战斗道具统一入口**：`dqOpenItems` 支持恢复类 + 暗器（`effect.dmg` 伤存活首敌、毙敌即时 `dqFinish`），含教学步进拦截；原 `useItemInCombat` 暗器能力无损迁移。
- 引擎 `getStatus` 保留 `playerUnits`/`enemies`（`player`/`enemy` 旧字段仅引擎内部日志快照自用）；测试删除 playTurn section（45/45 仍通过）。版本 bump `20260827j → 20260828k`，`?v=` 缓存参数同步。

---

### 9.26 货郎手机端手感优化 + 代码清理（v20260831m – v20260831o）

**v20260831m · 货郎手机端滚动难操作优化**
- **根因**：左栏 33 个货品整片 `.shop-good` 的 `touch-action:none` 让手机端手指滑动无法滚动；两栏 `max-height:none` 撑长后靠整张弹窗滚动。
- **优化**：手机端 `@media` 把 `.shop-good`/`.shop-right .packcell` 的 `touch-action` 改为 `pan-y`（纵向滑动恢复滚动，横向拖拽与轻点选中仍由 JS 接管，互不冲突）；两栏 `max-height:56vh` 让 `.shop-scroll` 真正栏内滚动；滚动条加粗 14px + `overscroll-behavior:contain`，拇指好抓不误触外层。寄售/购买走浮框按钮（`data-sellgo` 已绑定），不依赖拖拽。

**v20260831n · 手机长按拖拽寄售 + 待售数量醒目**
- **拖拽纵向丢失修复**（用户实测反馈）：`pan-y` 下手指纵向滑动被浏览器滚动抢走。改移动端标准「长按 200ms 进入拖拽」——按住不动才拖（`setPointerCapture` + 临时 `touch-action:none` 锁定该格），轻滑（<10px）放行原生滚动，轻点仍正常选中；实测长按拖拽成功寄售、轻点仍弹浮框。
- **待售数量醒目**：待售格底部新增橙底 **「寄售 N 件」** 行（同类多次拖自动堆叠到同一格累计总数，原仅左上角小标签不易察觉）。

**v20260831o · 代码清理（冗余 / 死代码 / 调试接口）**
- 删除 `state.equips` 恒为空死分支（装备已统一在 `equipment` 槽渲染，`shared/index.js` 仅保留兼容字段避免 undefined）。
- `openTestChest` 调试接口加 `?dev=1` 守卫（与调试台一致；确认无任何正常游戏逻辑调用它，防生产环境误触）。
- `shared/story/skills.js` 的 `LF.SKILLS` 武学树加「未接入任何升级/学习 UI」标注（真正驱动战斗的是 `state.learnedMartial`，避免维护者误判已生效）。
- 排查结论：交易系统结算（`confirmTrade` 用 `p.price`、银两不足整单阻止、买入入包/卖出按收购价）正确；离开货郎 `openModal`/`closeModal` 均 `restoreTradePending` 归还寄售物，无物品丢失隐患。

### 9.27 文件冗余清理 + 隐藏 bug 修复（v20260831p）

**文件清理（彻底清理方案，用户确认）**
- 删除明显无用：空目录 `shared/narrative/`、根目录调试脚本（`_fmt.py`/`_fmt2.py`/`_jscheck.py`/`_syn.py`）、日志（`srv.err`/`srv.log`）、旧版对照（`_msg.txt`/`_newshop.txt`/`_origshop.txt`）、23 个 `v*-changelog.html` 变更日志。
- 归档保留：8 个 `proto-*.html` 原型 + 6 个根目录 `.md` 工作笔记（`combat-audit`/`design_vs_code_diff`/`新手教程重设计`/`开场复盘`/`路线与复查`/`制作表单`）→ `_archive/`（不删，留底备查）。
- 删除 `assets/` 未引用历史资源（实际仅 `_pv_ref.png`/`title_bg.jpg`/`impacts/` 被代码引用）：`map_real/map_ai/map_inkwash/map_parchment/map_ref/_ref_orig/_pv_ref2/_pv_small/reforig/refp` 等 `.png/.jpg`，及 2 个 `_ne_*.geojson` 大文件（约 10MB）。
- 核查：`_game_inline.js`/`_del_beggar.js` 实际文件已不存在（仅本文历史提及），无需删；`serve_nocache.py`/`verify_playtest.py` 保留作本地验证。

**隐藏 bug 修复**
- 「试炼宝箱」房间（`shared/story/rooms.js`）直接调用 `openTestChest()`，而该全局函数上一轮已改为仅 `?dev=1` 才暴露——生产环境下点击会抛 `ReferenceError`。加 `typeof openTestChest==='function'` 守卫，非 dev 模式静默不报错。

---

*最后更新：2026-08-31（§9.26–§9.27：代码清理 + 文件冗余清理 + 试炼宝箱 ReferenceError 修复，v20260831p）*

### 9.28 数据交叉引用校验脚本 + 13 处悬空引用修复（v20260831q）

**新增校验脚本 `test/cross_reference_check.js`**（15 条规则）
- 覆盖：房间出口/NPC、敌人掉落、配方、商店、蓝图、物品蓝图、事件物品、门派武学、武学艺线、触发器、源码调用点（startCombat/move/talk/packAdd/openModal 等）、城市势力、ID 唯一性、装备槽位。
- 智能提示：悬空引用自动给出「笔误/简写/命名不一致」候选；退出码 0=通过 / 1=有 ERROR / 2=仅 WARN。
- 运行：`node test/cross_reference_check.js`（首跑即发现 13 ERROR + 1 WARN，全部人工核实为真实 bug）。

**修复的悬空引用（13 处 ERROR）**
- **房间 NPC**（`shared/story/rooms.js` camp_yard）：`'zhou_tingtao'` → `'zhoutingtao'`（与 dialogues/triggers 命名统一，修复劳役场周听涛不显示）；删除无任何定义的残留 `'hu_shi'`（设计稿中劳役场孙老/牛铁等未实现，当前精简版仅保留周听涛）。
- **敌人掉落**（`shared/data/items.js` 补 5 个缺失物品定义）：`blade_manual_frag` 刀谱残页 / `talisman_scrap` 残符 / `halberd_manual_page` 画戟谱残页 / `war_horse_token` 战马令 / `heishan_token` 黑山令——修复流寇头目/太平力士/华雄/黑山寨主战利品永远无法入包。
- **冶炼工坊**（`shared/data/build.js`）：`kuangshi`→`tiekuangshi`、`tieding`→`tiekuai`（6 处），修复「请他熔石」「熔石取铁」失效。
- **酒楼/马市/书肆**（`index.html`）：`packAdd('jian')`→`packAdd('zhujian')`；补 `jiu` 黍酒、`horse` 川马物品定义，修复三处店肆购物买到无名空物品。

**疑点处理（1 处 WARN）**
- `CITIES.owner` 短名（yuan/lu/liu/ma/sun/kongrong/shixie）与 `FACTIONS` 全名不一致，且与 `CITY_OWNER` 有 7 处冲突（颍川/昌邑孔融vs曹操、下邳吕布vs曹操、渔阳/蓟城公孙度vs袁绍、寿春袁绍vs曹操、晋阳董卓vs马腾、番禺士燮vs汉室、许昌曹操vs汉室）。已按 `cityDefaultOwner` 的优先顺序（CITY_OWNER > c.owner）将 `cities.owner` 全部对齐为 `CITY_OWNER` 值，消除双源冲突。

**版本/缓存同步**
- 版本 `20260831p` → `20260831q`（`shared/config/constants.js`）；`index.html` 全部 23 处 `?v=` 缓存参数统一为 `20260831q`（此前残留 14 个旧版本号，存在缓存不刷新的隐患）。

**验证**
- `node test/cross_reference_check.js` → 0 ERROR / 0 WARN，全绿。
- 全部 shared JS `node --check` 通过；`test/index_html_syntax_check.js` 0 错误；战斗引擎 45/45；营造系统全部通过。
- 浏览器实测：标题屏/图鉴/存档选择/捏人/建造测试场全部正常渲染，无控制台报错；运行时确认 65 物品、20 城、周听涛等 NPC 解析正常。

---

*最后更新：2026-08-31（§9.28：数据交叉引用校验脚本 + 13 处悬空引用修复 + 版本缓存统一，v20260831q）*
