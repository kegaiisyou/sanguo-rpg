# 新手流程：全分支跑通路线 & 复查报告（v0.3.5）

> 强制刷新后版本号应为 **v0.3.5**。本文件列出每条支线如何继续、已知 bug 修复与仍可优化的点。

---

## 一、本轮新增/修改的三项功能

### 1. 轻触快进（单击文字立即显示整段）
- 文字正在逐字输出时，**单击叙事区（#scene）**即可把当前这段瞬间显示完整。
- 仅作用于"正在打字的那一段"；**两次快进至少间隔 300ms**，防止手滑连点一次跳过好几段。
- `logScene` 段间有 150ms 停顿，停顿期无正在打字的段落 → 连点无效，天然做到"一段一击"。
- 打字时段落末尾有闪烁 `▾` 提示；开场弹出一次"轻触叙事文字，可立即显示整段"。

### 2. 文字速度默认调慢 + 设置可调
- 原默认 18ms/字（「常」），现默认 **55ms/字**（比旧「缓」46 还慢）。
- 设置里「文字演出」由 疾/常/缓 三档改为**滑块**：0=瞬（无动画），5–100ms 连续可调。
- 旧存档的 0/1/2 会自动迁移（1→22、2→55、0→瞬），不会突变。

### 3. 状态栏改为"问名"时才解锁
- **村口苏醒不再点亮状态栏**（CSS 确认：教学态下 `#status` 默认隐藏，仅 `reveal-status` 才显示）。
- 北巷点「老乞丐」问名，**三个选项各自解锁状态栏并解说**，好感分支：
  - ① 报名：`老乞丐健谈，详介状态栏`（气血/内力/时辰/年号），**好感 +1**
  - ② 反问朝代：老乞丐说不知，但可看状态栏知时间，**好感 -1**
  - ③ 沉默：老乞丐说"出门在外不该轻信他人"，但状态栏可查姓名时辰，**好感 -1**
- 问名后状态栏常驻，直至毕业（`body.onb` 移除后依旧显示）。

---

## 二、全分支跑通路线（每条支线都验证可继续，无死循环）

```
[新游戏] 捏脸/起名
  │
  └─▶ 燕山·村落口 (ys_entrance)         【onEnter：点亮 对话列表；状态栏【隐藏】】
        │  移动罗盘：仅「北」可走
        ├─(A) 不交谈直接「北」 ──────────────┐（状态栏仍隐藏，正常）
        └─(B) 点「老乞丐」交谈（intro_enter）┘→ 苏醒对话 → 解锁移动罗盘
  │
  └─▶ 燕山·北巷 (ys_north)               【onEnter：锁退路 fwd=ys_ne】
        │
        ├─(C) 不交谈直接「东北」 ──────────────┐（跳过问名亦可，状态栏暂隐藏）
        └─(D) 点「老乞丐」问名（intro_beggar_north，三选项）
              │
              ├─ ① 报名  ─→ 状态栏解锁 + 老乞丐详介 + 好感+1
              ├─ ② 反问  ─→ 状态栏解锁 + 好感-1
              └─ ③ 沉默  ─→ 状态栏解锁 + 好感-1
              │  （三选项均 reveal status，对话结束 → 可「东北」）
              └──────────────────────────────┘
  │
  └─▶ 燕山·东北猎棚 (ys_ne)              【onEnter：锁退路；触发教学战】
        ╔══════ 教学战斗（严格状态机，点错温和纠正、不死循环）══════╗
        ║ 高亮〔攻击〕→ 虚招落空，老乞丐救场，臂上擦伤，抛行囊(1瓶金疮药)
        ║ 高亮〔防御〕→ 防字诀演示
        ║ 高亮〔道具〕→ 金疮药回血
        ║ 高亮〔攻击〕→ 老乞丐送敌上路 → 胜利（tcDone）
        ║ 叛逆分支均可跑通：先防/狂攻/中途用药/战败救场 → 均收敛到胜利
        ╚═════════════════════════════════════════════════════╝
  │
  └─▶ 东→东溪桥 →东南→东南田垄 →南→南磨坊 →西南→西南谷场
       →西→西柴院 →西北→西北哨所
       （每段 onEnter 自动锁退路，仅「前进」方向可走，不会卡死）
  │
  └─▶ 燕山·山口 (yanshan_shankou)         【onEnter：逃脱收尾 intro_escape】
        │
        ├─ 好感 ≥ 1（走 ①报名） ─→ 老乞丐留守山口（可常谈）
        └─ 好感 < 1（走 ②反问 / ③沉默 / 全程跳过问名）─→ 老乞丐离去
        │  两条都 → flags.onb.done=true → 毕业（解锁完整 UI，状态栏常显）
  │
  ▼
[开放世界] 白檀屯寨 → 渔阳 → 洛阳 → …
```

### 分支对照表（确保每条都"能继续"）
| 支线 | 是否卡死 | 后续 |
|---|---|---|
| A 村口不交谈直接北 | 否 | 到北巷 |
| B 村口交谈 | 否 | 到北巷 |
| C 北巷不交谈直接东北 | 否 | 触发教学战（状态栏暂隐，毕业才显） |
| D① 报名(+1) | 否 | 状态栏显+老乞丐留山口 |
| D② 反问(-1) | 否 | 状态栏显+老乞丐离去 |
| D③ 沉默(-1) | 否 | 状态栏显+老乞丐离去 |
| 教学战：狂点攻击 | 否 | 纠正→走防御→道具→收尾 |
| 教学战：先点防御 | 否 | 纠正→先攻击→… |
| 教学战：中途用药 | 否 | 跳过专属道具步→攻击→防御→收尾 |
| 教学战：战败 | 否 | 老乞丐救场，tcDone，继续 |
| 山口好感≥1 / <1 | 否 | 均毕业进开放世界 |

---

## 三、复查发现的遗留问题 & 优化点

### 已顺手修复
- 状态栏问名解锁时高亮不可见 → 已补 `#status.onb-glow` 样式。

### 仍可优化（未动，按需再做）
1. **问名可设为"必经"**：当前 C 支线（北巷不交谈直接东北）会跳过问名，状态栏要等到毕业才显示。若希望状态栏一定在教学期就出现，可在 `intro_chain_gate` 于 `ys_ne` 强制要求 `tcDone` 前先完成问名（但会牺牲"可跳过"的自由度，暂未做）。
2. **战斗内文字也可快进**：当前快进仅作用于 `#scene` 叙事区，教学战战斗卡内的老乞丐台词走 `log` 同样可快进，但战斗覆盖层可能拦截点击——若需要，可把 `skipTypewriter` 监听也挂到战斗卡。
3. **速度滑块加预设**：可保留「瞬/常/缓」快捷按钮，方便不习惯拖滑块的玩家。
4. **快进冷却提示**：连点被 300ms 冷却拦截时，可给一个极轻的"已到底"反馈，避免玩家以为点击失效。

### 经复查确认无 bug 的点
- 移动门禁每次进房按 `ONB_CHAIN` 重算 `fwd`，猎棚之后 9 段链一路可走到山口，不会卡死。
- 毕业移除 `body.onb` 后状态栏默认 `display:flex` 正常显示。
- 教学战胜利后 `tcDone=true`，回头路被门禁锁住，不会重触发教学战。
- `favor` 经 `applySet` 写入 `npcFavor['beggar_old']`，山口按 `min:1` 判定去留，与三选项好感设计一致。
- 飞镖已移除、金疮药固定 1 瓶，道具按钮仅在首击受伤后才出现。
