# 乱世烽火 · 游戏设计文档（文字放置版 v2）

> 本文档与 `PROJECT_GUIDE.md` 对齐。游戏定位已从「回合制 RPG（重美术/地图）」调整为「文字放置 RPG（弱化美术/地图，重文字叙事与数值成长）」。所有剧情文本与数值配置以 `shared/` 为唯一数据源。

---

## 一、游戏概述

| 项 | 值 |
|----|----|
| 游戏名称 | 乱世烽火 |
| 类型 | 东汉末年 · 文字放置 RPG（文字即美术） |
| 主端 | 网页 H5（根目录 `index.html`） |
| 分发端 | 微信小程序 / 小游戏（后期，复用 `shared/`） |
| 唯一数据源 | `shared/`（文字剧情 + 数值配置） |
| 时代背景 | 中平年间：黄巾之乱 → 讨董卓 → 群雄割据 |

---

## 二、核心设定

### 2.1 主角
颍川草莽「你」——未被史书记载的义军头目，因乡里遭黄巾劫掠而起事，逐步累积声望与武学。

### 2.2 核心循环（放置向）
```
文字事件 / 抉择  →  数值成长（修为 / 银两 / 门派进度）
      ↓
离线挂机收益  →  解锁新武学 / 新事件分支
```
- **历练**：部众替你操练，得修为/银两/内力（即时）。
- **探索**：触发随机事件，选项分支结算收益。
- **修炼**：消耗修为习得武学树节点（永久加成）。
- **休整**：恢复气血内力。
- **离线**：按离场时长累计收益，封顶 8 小时。

---

## 三、数值与成长（`shared/config/`）

| 项目 | 公式 / 取值 | 来源 |
|------|------------|------|
| 升级所需修为 | `60 × 1.6^(level-1)` | `BALANCE.expNeed` |
| 历练即时收益 | 随等级与攻防成长 | `BALANCE.trainYield` |
| 离线收益 | 按分钟累计 × (1+等级系数)，封顶 8h | `BALANCE.offlineGain` |
| 存档 | `localStorage` + `lastSeen` 时间戳（根 `index.html` 内联） |

---

## 四、门派（`shared/story/sects.js`）

| 门派 | 倾向 | 加成 | 初始武学 |
|------|------|------|----------|
| 颍川义军 | 攻守均衡（新手） | 攻+2 / 防+2 / 气血+20 | 基础拳脚 |
| 太平道 | 内力/奇术 | 内力+30 / 攻+1 | 基础拳脚、吐纳法 |
| 西凉军 | 攻击/血厚 | 攻+4 / 气血+40 / 防+1 | 基础拳脚 |

---

## 五、武学树（`shared/story/skills.js`）

| 武学 | 阶 | 前置 | 耗修为 | 效果 |
|------|----|------|--------|------|
| 基础拳脚 | 1 | 无 | 0 | 攻+1 / 防+1 |
| 吐纳法 | 1 | 无 | 10 | 内力上限+15 |
| 颍川刀法 | 2 | 基础拳脚 | 30 | 攻+5 |
| 太平经 | 2 | 吐纳法 | 30 | 内力上限+30 / 攻+2 |
| 霸王枪 | 3 | 颍川刀法 | 120 | 攻+12 / 防+3 |

---

## 六、随机事件（`shared/story/events.js`）

5 个颍川背景事件，含抉择分支：`溃兵劫掠`、`水镜点拨`、`左慈赠药`、`西域行商`、`县尉托付`。每个事件选项即时结算（`effect` / 可选 `cost`），文字优先于数值。

---

## 七、序幕与 NPC（`shared/story/dialogues.js`）

- **序幕**：中平年间颍川郡，黄巾肆虐，草莽起兵。
- **县尉韩伯**：序章引子与剿匪托付。
- **医馆华老**：游方医者，售药。
- **义军校尉**：引向西凉大营、华雄。
- **方士左慈**：行气导引，恢复气血内力。
- **水镜先生**：点拨，内力上限+20。
- **华雄**：序章叙事 BOSS（史实为董卓部将）。

---

## 八、废弃设定

- **幽州大营越狱线**（地牢/劳役场/兵营/武库粮仓/辕门、幽州督军 BOSS）整体废弃，不在统一设定中使用。
- 原 `wechat-game` 的 Canvas 地图与回合战斗表现层，在文字路线下**暂不使用**；仅其颍川线**文本**进入 `shared/story/`。

---

## 九、技术说明

- 纯 DOM/CSS 文字渲染，无游戏引擎，最契合大量文字与高频迭代。
- `index.html` 已禁缩放/下拉（viewport + `overscroll-behavior:none` + 容器 `100dvh` + 拦截 `touchmove`），体验接近原生 App。
- 共享层为 UMD：浏览器挂 `window.LF`，Node/微信端走 `module.exports`，三端共用一份。
- 微信端（小程序/小游戏）：H5 跑通后，用 WebView 套壳或重写文字 UI，引用同一份 `shared/`。

---

*文档版本: v2.0（文字放置版）*
*最后更新: 2026-07-20*
